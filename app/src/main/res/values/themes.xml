<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Cropscan" parent="Theme.MaterialComponents.DayNight.NoActionBar">
<!--        <item name="android:background">#000000</item>-->
        <!--设置导航栏/状态栏窗口color为透明-->
        <item name="android:navigationBarColor">@color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
        //透明的状态栏
        <item name="android:windowTranslucentNavigation">true</item>//透明的导航图
        <item name="android:fitsSystemWindows">true</item>//设置应用布局时是否考虑系统窗口布局
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/btn</item>
        <item name="colorPrimaryDark">#ffffff</item>
        <item name="colorAccent">#6E6E6E</item>
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="Theme.Cropscan.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Cropscan.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <!-- Dialog的配置属性 -->
    <style name="LoadDialog" parent="android:Theme.Dialog">
        <!-- 背景颜色及透明程度 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 是否半透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 是否没有标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否背景模糊，这个决定背景是否灰色 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 设置背景模糊的透明度-->
        <item name="android:backgroundDimAmount">0.5</item>
    </style>

    <style name="DialogCropBottomTips" parent="@android:style/Theme.Dialog">
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 模糊 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 遮罩层 -->
        <!--        <item name="android:backgroundDimAmount">0.5</item>-->
    </style>
    <!-- dialog的动画 -->
    <style name="main_menu_animStyle">
        <item name="android:windowEnterAnimation">@animator/dialog_in_anim</item>
        <item name="android:windowExitAnimation">@animator/dialog_out_anim</item>
    </style>
</resources>